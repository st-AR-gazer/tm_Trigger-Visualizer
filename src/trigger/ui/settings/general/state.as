namespace TriggerVisualizer {
    namespace Trigger {
        namespace Ui {
            [Setting hidden name="Trigger: Render world overlay"]
            bool S_RenderWorld = true;
        }
    }
}
